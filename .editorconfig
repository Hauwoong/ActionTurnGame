root = true

# 한글 주석이 CP949 로 저장되는 것을 막는다.
# 한글 Windows 의 Visual Studio 는 인코딩 표시가 없는 파일을 시스템 코드페이지로
# 추측해 저장하므로, BOM 을 붙여 UTF-8 임을 명시한다.
# Unity 는 BOM 이 있는 UTF-8 을 정상 처리한다.
[*.cs]
charset = utf-8-bom
